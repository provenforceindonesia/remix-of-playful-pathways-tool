// Placeholder — konten akan ditempel oleh user.
export function TimeStudyFormDialog({
  open,
  onOpenChange,
}: {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}) {
  void open;
  void onOpenChange;
  return null;
}
